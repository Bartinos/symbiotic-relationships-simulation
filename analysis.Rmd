---
title: "Untitled"
output: word_document
date: "2025-11-29"
---

```{r setup, include=FALSE}
data = read.csv("exp.csv", header = TRUE)
data
```

```{r}


library(ggplot2)
library(dplyr)
data %>%
  # group_by(RunId) %>%
  ggplot(aes(x = Step, y = Frogs, color=RunId, group=RunId)) +
    geom_line() 
```

```{r}

# where step == 499 -> if Frogs > 0
data %>%
  filter(Step == 499) %>%
    filter(Frogs > 0) %>%
      select(where(is.numeric)) %>%
      show
      # geom_col(aes(question, mean))
      # summarize(avg = mean(Frog_Symb_Val)) %>%
      # # geom_bar(data=avg)
      # show()



```
